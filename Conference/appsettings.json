{
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    }
  },
  "AllowedHosts": "*",

  "JwtSettings": {
    "Secret": "supersecurestring",
    "LifetimeSeconds" :  86400 
  },

  "ConnectionStrings": {
    "db": "server = localhost; database = conference; user = confuser; password = conf"
  }
}
